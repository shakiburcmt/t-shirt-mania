import React from 'react';
import AAA from '../AAA/AAA';

const A = ({ring}) => {
    return (
        <div>
            <h2>A</h2>
            <section>
                <AAA ring={ring}></AAA>
            </section>
        </div>
    );
};

export default A;